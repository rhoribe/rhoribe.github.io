# Testing

Run npm run format:check, npm run lint, npm run typecheck, npm run test, and npm run build. Run npm run test:e2e when browser binaries are installed. Manually review Chromium, Firefox, and a mobile browser at 320px, 375px, 768px, 1024px, and 1440px in light/dark themes with keyboard and reduced-motion preferences.
