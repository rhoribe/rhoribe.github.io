# GitHub Pages

Set Pages source to GitHub Actions, configure ricardo.horibe.com.br, verify DNS, and enforce HTTPS. The workflow publishes only out/ after all quality jobs succeed. After deployment, inspect asset paths and canonical URLs at the custom domain.
